export function AppNavbarLayout({ children }: { children: React.ReactNode }) {
  return (
    <div>
      {/* Navbar */}
      <div className=""></div>

      {/* Content */}
      {children}
    </div>
  );
}
